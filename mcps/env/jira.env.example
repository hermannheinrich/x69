# Copy to the local Jira MCP directory as .env. Never commit the filled file.
JIRA_PAT=<your-jira-personal-access-token>
JIRA_BASE_URL=https://<jira-host>/jira
HTTP_PROXY=http://<proxy-host>:<http-port>
HTTPS_PROXY=http://<proxy-host>:<https-port>
MCP_TRANSPORT=http
MCP_PORT=8000
